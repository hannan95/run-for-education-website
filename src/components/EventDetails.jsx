export default function EventDetails() {
  return (
    <section id="event" className="alt">
      <div className="container">
        <p className="eyebrow">Event Details</p>
        <h2 className="section-h">Race day</h2>
        <p className="lede">
          Venue, start time, and participating partner cities are still being finalized —
          nothing's confirmed yet. Register now and we'll email you the full details the moment
          they're locked in.
        </p>
      </div>
    </section>
  )
}
